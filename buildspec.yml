version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 22
    commands:
      - echo Installing dependencies...
      - npm install
  build:
    commands:
      - echo Build phase...
      - npm run build || echo "No build script defined"

artifacts:
  files:
    - '**/*'